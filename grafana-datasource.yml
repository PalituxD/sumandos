datasources:
- name: Prometheus
  type: prometheus
  access: direct
  url: http://${ENV_COLLECTOR_SERVER}:${ENV_COLLECTOR_PORT}
  basic_auth: false
  with_credentials: false
  is_default: true
  version: 1