scrape_configs:
- job_name: 'spring-actuator'
  metrics_path: '/actuator/prometheus'
  scrape_interval: 5s
  static_configs:
  - targets: ['${ENV_APPLICATION_SERVER}:${ENV_APPLICATION_PORT}']
  tls_config:
    insecure_skip_verify: true