scrape_configs:
- job_name: 'spring-actuator'
  metrics_path: '/actuator/prometheus'
  scrape_interval: 5s
  static_configs:
  - targets: ['${ENV_APPLICATION_SERVER}:${ENV_APPLICATION_PORT}','${ENV_APPLICATION_SERVER_2}:${ENV_APPLICATION_PORT_2}']
  tls_config:
    insecure_skip_verify: true